export interface IPendingCasesChart {
    order: number;
    groupDescription: string;
    count: number;
}

export interface ICasesCountByYear {
    flag: string;
    count: number;
    year: string;
}
